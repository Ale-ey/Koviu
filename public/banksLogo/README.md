# Bank logos

Place these image files here so they appear in the "Already Connected" section:

- `postfinance-logo.png`
- `zkb-logo.png`
- `raiffeisen-logo.png`

Supported formats: PNG, JPG, SVG (use `.png` or `.jpg` in the filename; the app looks for the names above).
